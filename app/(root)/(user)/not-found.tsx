"use client";

import {Button} from "@/components/ui/button";
import {RefreshCw} from "lucide-react";

export default function NotFound() {
    return (
        <div className="flex flex-col gap 2 items-center justify-center bg-background text-foreground h-full w-full text-center p-4">
            <h2 className="heading2 font-semibold ">Rất tiếc trang này chưa thể hiển thị</h2>
        </div>
    );
}
